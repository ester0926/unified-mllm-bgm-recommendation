# B5 smoke test：Stage 5 輸出投影層還原

- 產生時間：2026-07-26T20:38:13
- 有效配對樣本：1500（訓練 1200 / 留出 300）

## 問題

`stage5_preference_representation_v4.py` 的 `W_out_hybrid` 每次執行都重新 Xavier 隨機初始化，且未存入 `projection_weights.pt`、全檔無 `manual_seed`。直接重跑 Stage 5 會產生與訓練時不同的 LTP 空間，Persona 向量將無法與 exp_01 相容。

## 還原結果

| 指標 | 數值 |
|---|---|
| y 平均絕對值 | 0.999407 |
| 訓練集最大絕對誤差 | 4.265e-01 |
| 留出集最大絕對誤差 | 6.770e-01 |
| 留出集平均絕對誤差 | 4.390e-02 |
| 相對誤差 | 4.3930% |
| 留出樣本誤差 < 1e-4 之比例 | 0.0% （0/300）|

**判定**：還原失敗（門檻：留出集最大絕對誤差 < 0.001）

逐樣本最大誤差分位數：p50=1.363e-01、p75=1.847e-01、p90=3.133e-01、p95=3.779e-01、p99=4.738e-01

殘差未達門檻，代表 x 的重算仍與原始流程有出入（可能的原因：music_embeddings 的雙索引先到先存順序、semantic_seed 內容、或 profiles 版本不一致）。

→ **B5 不可直接沿用 Stage 5**。替代設計：改以 Persona 歷史曲目的既有 LTP 向量加權平均產生 Persona LTP，此法保證落在訓練分布內，但無法宣稱走完整條 Stage 3–5 管線。
